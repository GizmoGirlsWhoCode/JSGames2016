var r = 0;
var g = 0;
var b = 240;

var x = 720;
var i;

var back = new Rect(0, -50, 790, 850).fill('#0000F0').addTo(stage);

for (i = 0; i < 24; i++) {
  var rect = new Rect(0, -50, x, 850).fill("rgb(" + r + ',' + g + ',' + b + ')').addTo(stage);
  b -= 10;
  g += 10;
  x -= 10;
}

for (i = 0; i < 24; i++) {
  var rect = new Rect(0, -50, x, 850).fill("rgb(" + r + ',' + g + ',' + b + ')').addTo(stage);
  g -= 10;
  r += 10;
  x -= 10;
}

for (i = 0; i < 24; i++) {
  var rect = new Rect(0, -50, x, 850).fill("rgb(" + r + ',' + g + ',' + b + ')').addTo(stage);
  r -= 10;
  b += 10;
  x -= 10;
}
